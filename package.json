{
  "name": "deskprite",
  "version": "0.3.2",
  "description": "Draw animated desktop buddies or import transparent PNG sprite sheets with Deskprite.",
  "author": "Justin Uurtsaikh",
  "homepage": "https://justin-uurtsaikh.github.io/Deskprite/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Justin-Uurtsaikh/Deskprite.git"
  },
  "bugs": {
    "url": "https://github.com/Justin-Uurtsaikh/Deskprite/issues"
  },
  "keywords": [
    "electron",
    "desktop-pet",
    "desktop-sprite",
    "sprite-animation",
    "sprite-sheet",
    "pixel-art",
    "png"
  ],
  "main": "main.js",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "electron .",
    "start": "electron .",
    "icon": "node scripts/generate-app-icon.js",
    "demo-gif": "node scripts/generate-demo-gif.js",
    "test": "node --test",
    "check": "node --check main.js && node --check preload.js && node --check src/core.js && node --check src/creator.js && node --check src/pet.js && node --check docs/assets/site.js && node --check scripts/generate-app-icon.js && node --check scripts/generate-demo-gif.js && node --check test/core.test.js && node --check test/seo.test.js && npm test",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^44.3.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.deskprite.app",
    "productName": "Deskprite",
    "artifactName": "Deskprite-${version}-portable.${ext}",
    "directories": {
      "output": "release"
    },
    "files": [
      "main.js",
      "preload.js",
      "src/**/*",
      "package.json"
    ],
    "win": {
      "icon": "build/icon.png",
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "mac": {
      "category": "public.app-category.entertainment",
      "icon": "build/icon.png",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ]
    }
  }
}
